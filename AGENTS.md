# Agent 執行規則

## 環境檢查
- 執行任何 git 指令前,先確認 `git` 指令可用(例如 `git --version`)。若出現「無法辨識 'git'」等錯誤,代表是 PATH/環境問題,應立即停止並回報使用者,不要改用其他方式(如逐一手動上傳檔案)硬是繞過去。

## Git / Push 相關
- 執行 push 前,先用 `git status` 列出所有異動檔案(新增/修改/刪除),一次性確認清單,不要分批猜測。
- 使用 `git add -A`(或明確列出所有相關檔案)後,**一次性** commit + push,不要分多次推送。
- 若 push 失敗或不確定範圍(例如尚未 `git init` 或沒有設定 remote),先向使用者回報目前狀態並詢問,而不是自行重新分析整個專案架構、反覆嘗試。
- 禁止在同一任務中反覆執行「重新分析架構 → 重新推送」的迴圈超過 1 次;如遇到不確定,直接停下來詢問使用者。

## Token 使用效率
- 避免重複讀取已經讀過的檔案內容。
- 避免對整個 repo 做不必要的全文搜尋或掃描。
- 指令輸出盡量用 `--quiet` 或過濾,不要把冗長 log 全部塞進對話。
